<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.admin.pium.mapper.AdminMapper">

    <select id="findByUsername" resultType="com.admin.pium.entity.Admin">
        SELECT id, username, password, name, created_at, updated_at
        FROM admin
        WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.admin.pium.entity.Admin">
        SELECT id, username, password, name, created_at, updated_at
        FROM admin
        WHERE id = #{id}
    </select>

    <update id="updatePassword">
        UPDATE admin
        SET password = #{newPassword},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
